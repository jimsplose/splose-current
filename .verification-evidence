/clients/cmo6zika2002x6owgg3jjip9q

Session 21 — HintIcon tone + size props

Verified 3 HintIcon instances on client detail page:
1. Associated contacts heading — default tone, md (14px), color rgb(65,69,73) — muted grey ✓
2. Account balance header — tone=inverted, size=lg (20px), color rgba(255,255,255,0.8) — white on purple ✓
3. They owe row — tone=inverted, md (14px), color rgba(255,255,255,0.8) — white on purple ✓

Structural check: all 3 info-circle icons visible, correct sizes, correct colors.
No inline style={{ on any HintIcon in ClientDetailClient (done-when grep = 0).
tsc 0 errors. Build passed.

Verified: 9 properties measured (color×3, fontSize×3, marginLeft×3)
