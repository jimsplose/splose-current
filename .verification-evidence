Session 26b — Tailwind layout utilities + ClientDetailClient re-migration — 2026-04-22

## Chrome MCP measurements (1440x900)

### New utility classes in globals.css
- mb-0..8, mt-0..8, p-0/2/3/4/6, pt-2/4/6, pb-2/4, flex-1, shrink-0, w-full,
  max-w-2xl, overflow-hidden, overflow-y-auto, border-b
- `!important` added to margin/padding utilities to beat AntD's `:where()`-wrapped
  `.ant-flex { margin: 0 }` reset (specificity tie + cascade order lost without it)

### /clients/[id] (ClientDetailClient re-migration)

| Element | Class | Target | Actual | Pass? |
|---|---|---|---|---|
| Details/Edit Flex | mb-6 | 24px | 24px | PASS |
| Avatar Flex | mb-3 | 12px | 12px | PASS |
| Heading Text | mb-4 | 16px | 16px | PASS |
| Section | mb-8 | 32px | 32px | PASS |

### Pre-existing usage files (silent-bug fix)
- `/clients/[id]/practitioner-access` H1.mb-4: was 0px → now 16px (intended by author).
- `/clients/[id]/statements` Flex.mb-4: was 0px → now 16px.
- Similar for notes/new, invoices/batch-invoice — all now render intended spacing.

### ClientDetailClient inline-style count
- Before 26b: 62
- After 26b: 4 (aside right-panel, FileUpload size, 2 Tr table borders — all legitimate one-offs)
- Session 26 done-when (≤10): **PASS**

## TypeScript + Build
- npx tsc --noEmit: 0 errors
- npx next build: passed

Verdict: PASS. Session 26 done-when unblocked by landing 26b.
