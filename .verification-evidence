Session 30 — resolve duplicate TopNav — 2026-04-22

## Summary

Two files named `TopNav.tsx` existed:
- `src/components/TopNav.tsx` — splose-specific chrome (nav items + HeaderIcon component + 4 AntD icons + avatar SC placeholder)
- `src/components/ds/TopNav.tsx` — the DS primitive (renders logo + nav links + mobile menu + overflow)

**Discovery:** These are NOT duplicates — they compose. The splose component imports and wraps the DS component. The backlog's premise "DS TopNav has zero imports" was correct about src/app imports only — it IS imported by src/components/TopNav.tsx.

**Resolution:** Renamed `src/components/TopNav.tsx` → `src/components/SploseTopNav.tsx` so exactly one `TopNav.tsx` remains in the repo (the DS primitive). Updated `src/app/layout.tsx` import.

## Chrome MCP verification (1440x900)

| Element | Check | Result |
|---|---|---|
| <header> | rendered | PASS |
| Logo link | `href="/"` | PASS |
| Nav link count | 9 (Dashboard/Calendar/Clients/Contacts/Waitlist/Invoices/Payments/Reports/Products) | PASS |
| SVG icon count | 4 (Bell/Message/Settings/Question) | PASS |
| Header height | 57px (production ~56px matches) | PASS |

## TypeScript + Build
- npx tsc --noEmit: 0 errors
- npx next build: passed

## Done-when
- `find src -name 'TopNav.tsx' -not -path '*/stories/*' | wc -l` = 1 ✓
- `src/app/layout.tsx` imports TopNav from the surviving location (SploseTopNav) ✓
- `grep -r 'from.*TopNav' src/ --include='*.tsx'` shows only surviving-file imports ✓
- No visual regression ✓

Verdict: PASS. Pages verified: /
