/waitlist (Waitlist tab — tag badges in DataTable)

Verified: 4 badge properties measured on localhost:3000 vs production (acme.splose.com/waitlist screener).

| Property | Before fix | After fix | Production | Pass? |
|---|---|---|---|---|
| borderRadius | 4px (style override) | 8px (DS default) | 8px | PASS |
| fill style | light/outlined | solid (solid prop) | solid fill | PASS |
| text color | dark on light | rgb(255,255,255) white | white | PASS |
| fontSize | 12px | 12px | 12px | PASS |

Structural check:
- Localhost screenshot: taken — solid-fill badges visible in all 4 tag variants (red, yellow, teal, orange/medium, purple/NDIS)
- Missing/extra elements: none
- Layout diff: none — row heights, column widths, padding unchanged

---

## Session 13 — Badge size prop (2026-04-21)

### size="sm" — /waitlist service + tag chips (Chrome MCP, localhost:3000)

Measured via `getComputedStyle` on `.ant-tag` elements at 1440×900.

| Property | Expected (size="sm") | Measured | Pass? |
|---|---|---|---|
| padding | 4px 10px | 4px 10px | PASS |
| fontSize | 12px | 12px | PASS |
| borderRadius | 8px | 8px | PASS |

### size="lg" — InvoiceDetailClient:112 (Storybook fallback)

`InvoiceDetailClient.tsx` is not mounted on any current route (page.tsx renders invoice detail directly, not via InvoiceDetailClient). Storybook fallback per quality-gate.md Step 3a.

Storybook `Badge > Sizes` story verified:
- sm: padding 4px 10px, fontSize 12px (confirmed matches waitlist chips above)
- md: default AntD Tag padding, fontSize 12px
- lg: padding 6px 12px, fontSize 14px, fontWeight 500 — all three sizes render correctly in Storybook

Note: `InvoiceDetailClient:112` migration is correct code but the file is orphaned. No visual regression risk.

---

## Session 11 — ColorDot label + semantic colors (2026-04-21)

### /reports/progress-notes — ColorDot with label (Chrome MCP, localhost:3000)

Measured 4 ColorDot+label instances via `getComputedStyle` at 1440×900.

| Property | Expected | Measured | Pass? |
|---|---|---|---|
| dot width/height | 12px (sm) | 12px | PASS |
| green bg | rgb(0,194,105) | rgb(0,194,105) | PASS |
| purple bg | rgb(130,80,255) | rgb(130,80,255) | PASS |
| label fontSize | 14px | 14px | PASS |
| gap | 8px | 8px | PASS |
| alignItems | center | center | PASS |

Production note: acme.splose.com uses a 20px `_colorCircle` CSS module (different implementation). Our Status component used AntD Badge (~6-8px). ColorDot sm=12px is between these — pre-existing gap, not a regression.

### /clients/[id]/appointments — ColorDot no-label (Chrome MCP, localhost:3000)

| Property | Expected | Measured | Pass? |
|---|---|---|---|
| green dot width/height | 12px | 12px | PASS |
| green bg | rgb(0,194,105) | rgb(0,194,105) | PASS |
| gray dot width/height | 12px | 12px | PASS |
| gray bg | rgb(184,188,192) | rgb(184,188,192) | PASS |

Structural check: dots visible in When column, no layout shift, no console errors.

## Session 14 — FormInput/FormSelect disabled styling (2026-04-21)

**Route:** `/settings/details` — disabled fields: AUD (currency code), Country, A$ (currency symbol)

**Measurement method:** Chrome MCP `javascript_tool` on localhost:3000/settings/details

**Before fix:** Disabled inputs showed `backgroundColor: rgb(255,255,255)` (white) on localhost due to Tailwind CSS reset overriding AntD's default disabled styling. Three `style={{ backgroundColor: '#f3f4f6' }}` inline workarounds were present in settings/details/page.tsx.

**After fix:** `variant={disabled ? "filled" : undefined}` added to FormInput and FormSelect. Inline workarounds removed.

**localhost measurements (post-fix):**
- AUD input: bg `rgb(249,250,251)`, styleAttr null (no inline override)
- A$ input: bg `rgb(249,250,251)`, styleAttr null
- Country select: bg `rgb(249,250,251)` via `ant-select-filled ant-select-disabled` classes

**Production measurements:**
- AUD input: `rgba(0,0,0,0.04)` ≈ `rgb(245,245,245)` — light grey disabled fill
- Both production and localhost show visually equivalent light grey disabled backgrounds

**Done-when checks:**
- `grep -c "backgroundColor: '#f3f4f6'" settings/details/page.tsx` = 0 ✅
- tsc --noEmit: 0 errors ✅
- next build: passed ✅

## Session 15 — ColorDot shape/interactive/selected props (2026-04-22)

**Routes:** localhost:3000/settings/tags + localhost:3000/settings/online-bookings/1

**settings/tags — rect swatch (shape="rect" size="md"):**
- SPAN element, h:16px, w:80px, borderRadius:4px, bg:rgb(234,179,8) ✅
- Exact match to original inline `height:16, width:80, borderRadius:4, backgroundColor:tag.color`
- Production uses 60px/0px-radius (pre-existing fidelity gap, not a regression)

**settings/online-bookings/1 — interactive swatches (interactive, shape="circle", size="xl"):**
- 8 BUTTON elements, h:28px, w:28px, borderRadius:50%, cursor:pointer ✅
- Selected swatch: border `2px solid rgba(0,0,0,0.3)` ✅
- Unselected swatch: border `2px solid transparent` ✅

**Done-when checks:**
- `shape`, `interactive`, `selected` props present in ColorDot ✅
- 2 caller sites migrated (settings/tags + settings/online-bookings/[id]) ✅
- tsc --noEmit: 0 errors ✅
- next build: passed ✅
