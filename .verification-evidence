Session 26 — ClientDetailClient remaining cleanup (partial) — 2026-04-22

## Chrome MCP measurements (1440x900, localhost:3000)

### /clients/[id] — scope items complete
| Element | Property | Target | Actual | Pass? |
|---|---|---|---|---|
| Associated contacts table | element | DataTable | DS DataTable | PASS |
| Associated contacts Th count | 6 | 6 | PASS |
| Associated contacts rows | 2 | 2 | PASS |
| Th bg | DS default | rgb(243,245,247) | PASS (AdoptAsIs) |
| Tr border | 1px var(--color-border) | 1px rgb(231,232,232) | PASS |
| Section spacing | 32px mb | 32px mb | PASS |
| FeatureCard account balance | tone="primary" color="inverted" | in place | PASS |
| Icon usage | no raw `Outlined style` | grep = 0 | PASS |

## TypeScript + Build
- npx tsc --noEmit: 0 errors
- npx next build: passed

## Done-when
- Associated contacts migrated to DataTable ✓
- FeatureCard + inverted tones in place (from session 04) ✓
- No raw `<Outlined style>` in file ✓
- `grep -c "<table|<th style|<td style"` = 0 ✓
- `grep -c "style={{"` ≤ 10: **FAIL (62 remaining)**

## Partial — blocker

The ≤10 inline-style target requires Tailwind layout utilities (`mb-4`, `p-6`, `flex-1`, etc.)
which are NOT defined in this codebase. globals.css defines `text-*` typography utilities only
(see globals.css comment: "Previously provided by Tailwind; now defined explicitly after AntD migration").

Confirmed via live DOM test: `mb-8`, `mb-4`, `p-6`, `flex-1` on injected elements all produce 0px
margin/padding. Pre-existing usages in practitioner-access/page.tsx (mb-4 on h1), statements/page.tsx,
batch-invoice/page.tsx, notes/new/page.tsx are silently broken — adding those utilities would
visually regress those pages in an uncontrolled way.

Tried migration of 12+ layout margins to `mb-4/mb-8/mt-2` etc.; reverted after Chrome MCP showed
section margins went from 32px → 0px (section headings collapsing together).

All remaining 62 inline styles are legitimate layout one-off values (marginBottom / marginTop / padding /
flex / maxWidth / flexShrink / borderBottom) — CLAUDE.md explicitly permits these.

## Net reduction
- Before session: 81 inline styles
- After session: 62 inline styles
- Delta: -19 (table migration — 6 Th + 12 Td padding + 3 Tr border removed)

Pages verified: /clients/[id] (detail view)
