Work through the mockup enrichment work list. Read this file first:

1. Read `docs/mockup-enrichment-worklist.md` (the categorized work list)
2. Read `docs/agent-block.md` (subagent prompt template for UI work)

Ask the user which category to focus on:
1. No-op dropdowns (quick wins — wire existing dropdown actions)
2. Missing sub-pages (new/edit/view pages for contacts, products, invoices)
3. Modal enrichments (richer content in existing modals)
4. Interactive states (make static elements respond to clicks)
5. Multi-step flows (complete incomplete journeys)
6. Data enrichment (more realistic seed data)

Or let the user pick specific items. Launch parallel agents for independent items.
After each round, commit, push, and update the work list to mark completed items.
