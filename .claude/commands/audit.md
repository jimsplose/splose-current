Run a compare pages workflow. Read these files first:

1. Read `docs/compare-pages-workflow.md` (the procedure)
2. Read `docs/route-mapping.md` (production URL lookups)
3. Read `screenshots/screenshot-catalog.md` (current match status)

Check Chrome MCP availability. If unavailable, note that verification will use the fallback path.

Ask the user for scope: Quick (2-3 pages), Standard (all partials), Full sweep (every page), or Until done.

Then execute the workflow. Invoke `/impeccable:frontend-design` before comparisons.
