Show the current project status. Read these files and present a summary:

1. Read `docs/progress.md` — recent session summaries
2. Read `docs/fidelity-gaps.md` — open/closed gap counts
3. Read `docs/mockup-enrichment-worklist.md` — enrichment progress
4. Run `git log --oneline -10` — recent commits
5. Run `git status` — uncommitted changes
6. Check Vercel build status

Present:
- Last session summary
- Open gaps count and top priorities
- Uncommitted work
- Build/deploy status
- Suggested next steps

Then show the available workflow commands.
