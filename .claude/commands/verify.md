Run a compare pages workflow with Full sweep scope. Read these files first:

1. Read `docs/compare-pages-workflow.md` (the procedure)
2. Read `screenshots/screenshot-catalog.md` (current match status)
3. Read `src/lib/state-registry.ts` (all pages and state variants)

Execute the workflow at Full sweep scope — check every page. Use Chrome MCP for all screenshot capture. Invoke `/impeccable:frontend-design` before comparisons.
